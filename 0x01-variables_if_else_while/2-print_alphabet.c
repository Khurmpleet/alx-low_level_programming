#include <stdio.h>
/**
 * Main - Entry point
 *
 * Return: Alwys 0(Success)
 */
int main(void)
{
	char alpha ;
	
	for (alpha = 'a'; c<= 'z'; c++ )
	{
		putchar(alpha);
		putchar('\n');
	}
return (0);
}
