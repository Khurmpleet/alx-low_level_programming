#include <stdio.h>

/**
 * main - Program to print all single digit numbers of base 10 from 0
 *
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf("%i, number");
		putchar('\n');

	}
	return (0);
}
